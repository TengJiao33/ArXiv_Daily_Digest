# 研究方向笔记 — 价值对齐与表征干预

> 整理时间：2026-05-18
> 用途：内部研究路径备忘，配合 Research Radar 的 `representation-engineering` 方向使用
> 作者视角：本科生（应飞扬）已有 LEACE / DeBERTa / QLoRA 实战经验，目标 NLP 顶组保研

---

## 一、这条方向在解什么问题

LLM 已经被 RLHF 对齐过一遍，但 RLHF 解决不了 4 件事，**这就是这条方向存在的理由**：

1. **RLHF 太贵**：要构造偏好数据 + 训 reward model + PPO，对学术圈不可行
2. **RLHF 改一次只能锁一组价值**：想让模型 A 场景"避免暴力"、B 场景"创作惊悚小说"，RLHF 做不到 inference-time 切换
3. **对抗 prompt 一上来就破防**：明明对齐过，越狱 prompt 一句话就让它教做炸弹
4. **价值是隐式的、不可解释**：RLHF 训完不知道"诚实"被编码到哪里去了

**核心信念**：与其在外面用偏好数据训权重，不如进到模型残差流（residual stream）里直接找到"诚实方向 / 暴力方向 / 谄媚方向"那个向量，**推理时加减它**。

这条线是 representation engineering（RepE）大伞下的一支，**对算力极其友好**——大多数工作是 inference-only，单卡 4090 即可完成。

---

## 二、通用 5 步配方

几乎所有这条线的论文（ITI / CAA / RepE / ConVA / Conceptor / Tradeoffs / 黄民烈那篇）都遵循同一个 5 步骨架。

### Step 1：构造对比数据集（contrastive dataset）

> 给同一个问题，配一对回答：一个体现目标价值，一个违背目标价值

| 论文 | 数据形态 |
|---|---|
| **ConVA**（黄民烈 ACL2025）| Schwartz 10 个基础价值（self-direction / benevolence / achievement…），每价值几百个二选一情境 |
| **ITI** | TruthfulQA 上 truthful answer ↔ false answer |
| **CAA** (Panickssery 2023) | A/B 多选，A 体现行为 B 不体现 |
| **RepE** (Zou 2023) | prompt 模板 `Pretend you are extremely [X]. ...` 生成正负 pair |

数据规模：**几百到几千对**。这一步单人一周能搞定，不是瓶颈。

### Step 2：抽激活（activation extraction）

把正/负 prompt 都喂进模型，记录每一层残差流在最后一个 token 位置的激活向量。

```python
# 伪代码
positive_acts = []  # shape: [n_pairs, n_layers, hidden_dim]
negative_acts = []
for pos_prompt, neg_prompt in pairs:
    h_pos = model.forward(pos_prompt, output_hidden_states=True)
    h_neg = model.forward(neg_prompt, output_hidden_states=True)
    positive_acts.append(h_pos)
    negative_acts.append(h_neg)
```

**算力估算**：500 对 × 2 + Llama-3-8B + 单卡 4090 + fp16 ≈ 20 分钟。inference-only，无梯度。

### Step 3：识别"价值方向"（direction extraction）

> 在激活空间里找一个向量 v，沿着 v 走 = 价值越来越强

**这是技术分叉点**，不同论文从这里分流：

| 方法 | 怎么算 v | 数学形式 |
|---|---|---|
| **CAA**（最朴素）| 正样本激活均值 - 负样本激活均值 | `v = mean(h⁺) - mean(h⁻)` |
| **ITI** | 训线性探针分正负，取分类权重 | `v = w` (logreg coef) |
| **RepE** | PCA 第一主成分 | `v = top_PC(h⁺ − h⁻)` |
| **ConVA**（黄民烈）| Context-controlled identification：把同一价值放进多种语境，强制让 v 与"价值"相关而非与"语境"相关 | 多 context 间 partial out |
| **LEACE**（你已会）| 找正交投影矩阵 P，使任何线性分类器都无法从 P(h) 中识别该属性 | `P = I − W⁺W` (闭式解) |
| **Conceptor** (ICLR2026) | 从向量推广到**仿射变换** | `h ← M(h − μ) + μ` |

**和你 LEACE 工作的关系**：你 LEACE 干的是"去掉平台维度"，ConVA 干的是"找到价值维度"。**两个方向是对偶的**——LEACE 是 erase，CAA/ConVA 是 elicit。两者用的几何工具完全同一族（线性子空间投影），这是你套磁的天然连接点。

### Step 4：干预（intervention）

推理时在残差流上加 / 减这个向量 v。

```python
# 朴素加法（CAA）
h_layer_L += alpha * v   # alpha 控制强度

# ITI: 只在选定 head 上加
h_head_h += alpha * v_h

# ConVA gated: 加门控，只在"该话题确实涉及该价值"时才加
gate = sigmoid(W_g @ h)
h_layer_L += gate * alpha * v
```

**ConVA 的 gated activation 改进**：朴素 CAA 全程都加 v，导致模型聊菜谱时也强行体现 benevolence，性能下降明显。Gate 决定"何时加"——只在涉及价值判断的 token 处激活。这正面回应了 [Tradeoffs Between Alignment and Helpfulness](https://arxiv.org/abs/2401.16332) (ICML2024 spotlight) 指出的"steering 损害 helpfulness"问题。

### Step 5：评测

测 3 件事：
1. **控制成功率**（control success rate）
2. **对抗 prompt 鲁棒性**（adversarial robustness）
3. **通用能力是否退化**（MMLU / GSM8K / 流畅度）

ConVA 在 10 个 Schwartz 价值上比基线 ITI/CAA 高、对越狱 prompt 守得住、不退化。**这一步本身就是一种 benchmark**——黄民烈那篇相当于"自己定义了 ConVA-Bench"，这是"刷分"的另一种形态：**给新干预方法定一个新评测协议，然后用自己的方法拿第一**。

---

## 三、全谱地形（2024-2026 子流派）

把同方向工作分到 5 个子流派，看清谁在哪个位置：

### 流派 A：朴素方向加法（已成基线）
- **CAA** (Panickssery 2023, ICLR2024)
- **ActAdd** (Turner 2023)
- 现在不再是 paper 主体

### 流派 B：方向识别更精细（在 Step 3 上加创新）
- **RepE** (Zou 2023): PCA + multi-prompt
- **ITI** (Li NeurIPS 2023): top-K 注意力头 + linear probe
- **ConVA**（黄民烈 ACL2025）: context-controlled
- **LEACE** (Belrose NeurIPS 2023, **你已会**): 闭式正交投影
- **Conceptor** (ICLR2026): 仿射变换扩展

### 流派 C：干预更精细（在 Step 4 上加创新）
- **ConVA gated**（黄民烈 ACL2025）：门控
- **ODE-based steering** (2025): 把激活演化看作 ODE，用控制论方法
- **Linear Optimal Control** (2025): LLM 局部线性化 + LQR

### 流派 D：分析失败模式（专门挑刺）
- **Tradeoffs Between Alignment and Helpfulness** (Levine ICML2024 spotlight): steering 会损害基础能力
- **Analysing the Safety Pitfalls of Steering Vectors** (2026): refusal 方向其实 brittle
- **Scaling laws for activation steering** (2025): CAA 在大模型上效果衰减

> **本科生最友好的子流派**——不需要发明方法，只要找还没人挑过的失败模式。

### 流派 E：扩展应用领域（同样工具，新 target）
- **Persona steering** (Princeton 2025)
- **Refusal direction** (Anthropic 2024)
- **Risk preference steering** (2025-05)
- **Pluralistic value alignment** (2026)
- **Differentially private steering** (2025)

---

## 四、与目标老师的精确匹配

基于近 1-2 年实际论文清单（不是模糊画像）：

### 🟢 黄民烈（清华 CoAI，⭐⭐⭐⭐⭐ 最强匹配）

| 论文 | 子流派 | 与你的连接 |
|---|---|---|
| **ConVA** (ACL2025) | B + C | LEACE 几何工具同源；价值方向 vs 平台维度形式对偶 |
| **Internal Value Alignment via CVA**（同上） | B | 直接讲"在激活空间用线性方向操控价值表示" |
| **DELMAN: Dynamic Defense via Model Editing** (Findings ACL2025) | C | model editing 防越狱 |
| **ShieldLM** (Findings EMNLP2024) | E | 安全检测器 |
| **Probing Internals Behind Prompt Leakage Intents** (EMNLP2025) | D | 从内部状态预测越狱意图 |

**为什么搭**：黄民烈这条线是这批老师里**唯一同时覆盖 B/C/D/E 全谱**的。他的 ConVA 在方法上就是 LEACE 的孪生路线，套磁第三段可以直接讲"我做的跨平台 LEACE 去偏与您 ConVA 在激活空间用线性方向控制价值表示几何思路同源"。

### 🟢 王晓智（清华 SIGS，⭐⭐⭐⭐）

- 主页明示主线："**mechanistic interpretability + LLM safety + data-centric**"
- 2024 EMNLP "**Finding Safety Neurons**"——把安全行为定位到具体神经元，**这就是流派 B 在 neuron 粒度的延伸**
- 2025 ICLR "Precise Neuron-level Knowledge Editing"
- 发布 [OpenSAE](https://github.com/THU-KEG/OpenSAE) 工具包

**为什么搭**：safety neurons 与 value vectors 是**同一问题在不同粒度**（neuron-level vs layer-level）的解。你 LEACE 是 layer-level，进他组可以学 neuron-level，研究上是连贯的而非重复。

### 🟢 潘亮铭（北大 PILLAR，⭐⭐⭐⭐）

- 主线："**Mechanistic Study of LLMs + Trustworthy LLMs Guided by Mechanism**"
- 2026 ACL 3 篇（intrinsic interpretability、RL mechanisms、reverse LLMs）+ 2026 ICML 2 篇（含 Spotlight，关于 mechanistic data attribution）
- 2025 EMNLP Oral "**How Is LLM Reasoning Distracted by Irrelevant Context**"

**为什么搭**：他的范式是"先理解机制再改进模型"，与表征干预的 elicit-then-intervene 思路同构。**他更偏 reasoning 不是 value alignment**，但 representation-level intervention 是双方共同语言。

### 🟡 杨耀东（北大 Alignment Group，⭐⭐⭐）

- 2025 ACL "**Language Models Resist Alignment: Evidence From Data Compression**"
- SafeRLHF / Aligner / Superalignment / Elasticity of Alignment

**为什么搭**：与你 LEACE 形式接近（都讲数学/几何视角的对齐），但他主线是 **RL + 数据压缩**，不是激活干预。**作为冲北大对齐方向的二级目标**而非首选。

### 🟡 桂韬（FudanNLP，⭐⭐⭐）

- 主线已转向 **AgentGym + RLHF + reasoning**
- 与表征干预交集不大，但 RLHF 大组的 alignment 资源强

### 🔴 钱忱（SJTU AAIL）/ 蒋智威（南大）/ 孙栩（北大）

- 钱忱：multi-agent system，不在这条方向上
- 蒋智威：sentence embedding + inference-time steering，技术接近但不是 alignment
- 孙栩：近期已转 video/multimodal/financial NLP，mech 不再中心

---

## 五、我们能做什么 — 3 个口子

3 个具体口子按**可行性 + 议价权**排序。每条都标注算力 + 投稿场地。

### 口子 1：LEACE-then-CAA 的方向识别横评（"刷分"形态最干净）

**做什么**：在统一任务集上把 CAA / ITI / RepE / ConVA / **LEACE** / Conceptor **6 种方向识别方法**横评，争 SOTA。
- target: persona steering / refusal / value alignment / truthfulness 中选 1-2 个
- model: Llama-3-8B、Qwen-2.5-7B、Gemma-2-9B
- 评测: 控制成功率 + helpfulness 保留 + 对抗鲁棒性

**核心 finding 假设**：LEACE 的"闭式正交投影"在去除冗余维度上比 CAA/ITI 干净，但单独用作 elicit 不如 CAA。**所以提一个 LEACE-then-CAA 的两阶段方法**——先用 LEACE 去掉无关维度，再在干净空间里做 CAA。新方法但难度低。

**为什么我们能做**：你已经实现过 LEACE，复现 CAA/ITI/RepE 都是开源代码（每个不超 200 行）。Conceptor 论文有 OpenReview，复现可控。

**算力**：单卡 4090 + 4 个开源模型 + 6 种方法 = 3-5 天跑完
**投稿**：BlackboxNLP / EMNLP findings / ACL findings
**对接**：黄民烈、王晓智、杨耀东、潘亮铭（**4 个老师同时命中**）
**议价权**：⭐⭐⭐⭐⭐
**学术风险**：中（LEACE-then-CAA 的 finding 不一定显著）

### 口子 2：流派 D，专门挑 ConVA 的刺

**做什么**：复现 ConVA，然后在它没测过的设定下找它崩盘的位置。
- ConVA 只测了英文 + 10 个 Schwartz 价值；**中文情境 + 跨文化价值**（孝道、面子、集体主义）下表现如何？
- ConVA 的 gate 在长上下文下是否还有效？
- ConVA 在 multi-turn 对话中是否会发生"价值漂移"？

本质：**给已有方法做更严格 benchmark**——不发明新方法，发明新评测设定。"分数"是 ConVA 在你新设定上的崩盘程度。

**为什么我们能做**：复现 ConVA 已开源（[github.com/hr-jin/ConVA](https://github.com/hr-jin/ConVA)）。中文价值数据可基于 World Values Survey 中国部分 + 自构造。你 SiNet 工作里已经做过中文细粒度标注，方法学迁移成本低。

**算力**：API + 4090，1-2 周
**投稿**：EMNLP findings / NLPCC
**对接**：黄民烈本人（直接续他工作）
**议价权**：⭐⭐⭐（绑定单一老师，但绑得很紧）
**学术风险**：低（找 bug 必有 finding）

### 口子 3：流派 E，扩展到中文情感场景

**做什么**：把激活引导用到**评论文本的情感强度可调控生成**——给定中性评论，沿"愤怒方向"加 0.5 / 1.0 / 1.5 倍，看模型生成强度是否真的可控。

**为什么我们能做**：直接接你**论文一的 SiNet 连续情感量化**——你已经会量化情感，现在用激活引导做反向（指定情感强度让模型生成）。是叙事最连贯的一条。

**算力**：单卡 + API
**投稿**：EMNLP findings / NLPCC
**对接**：黄民烈（情感 + steering 双线命中）
**议价权**：⭐⭐⭐
**学术风险**：中（情感强度生成已有零散工作，新颖度需要查明）

---

## 六、风险与注意事项

### 6.1 不要现在就锁路线
3 个口子里任何一个都不是现在该决定的。决定权应交给雷达——下一步把 RepE direction 关键词扩了之后，跑 2-3 周让雷达扫一遍这条方向 2024-2026 的论文，会得到一份很具体的"已做完什么 / 还没人做什么"清单，再选具体口子最稳。

### 6.2 警惕"steering 不 work" 这件事
[Tradeoffs Between Alignment and Helpfulness](https://arxiv.org/abs/2401.16332) 这篇 ICML2024 spotlight 已经系统地批判了 steering 损害基础能力。如果你做口子 1，**必须把 helpfulness 保留作为评测维度之一**，否则会被 reviewer 直接拒。流派 D 系列论文（"Safety Pitfalls"、"Scaling laws"）也都在挑这一类毛病。

### 6.3 警惕和 LEACE 原作组撞车
LEACE 原作 EleutherAI 在持续做 erasure 系列。你做的工作必须在他们的工作基础上有清晰增量——比如**把 erasure 和 elicit 显式组合**（这正是口子 1 的位置），而不是只复现 LEACE 在新场景上。

### 6.4 套磁信叙事
对黄民烈：直接讲 LEACE 与 ConVA 的几何同源。
对王晓智：讲你想从 layer-level 去偏走到 neuron-level safety neuron 这一更细粒度。
对潘亮铭：讲 representation-level intervention 是机制理解的下游，与他"先理解再改进"范式同构。
对杨耀东：讲数学/几何视角的对齐——但要承认你是 inference-time intervention 路线，不是 RL 路线。

---

## 七、雷达对接

`config/directions.yaml` 中 `representation-engineering` 方向已扩展，关键词新增：
- `ConVA / value vector / refusal direction / persona steering / activation editing / inference-time alignment / safety steering / contrastive activation`

种子论文新增：
- `2507.11316` — ConVA (Jin, Huang ACL2025)
- `2306.01693` — ITI (Li et al. NeurIPS 2023)
- `2401.16332` — Tradeoffs Between Alignment and Helpfulness

`max_papers` 从 20 提到 30。

其他三个方向（truthfulness / mechanistic-interpretability / 雷达整体架构）保持原样。

执行频率：每日抓取 + 每周日 landscape，照旧。

---

## 八、推荐阅读清单（按读取顺序）

1. **Belrose et al. NeurIPS 2023** "LEACE: Perfect linear concept erasure in closed form" — 你已经在用
2. **Zou et al. 2023** "Representation Engineering: A Top-Down Approach to AI Transparency" — 整条方向的开山综述
3. **Li et al. NeurIPS 2023** "ITI: Inference-Time Intervention" — 经典 baseline
4. **Panickssery et al. ICLR 2024** "CAA: Steering Llama 2 via Contrastive Activation Addition" — 简单版
5. **Jin, Huang et al. ACL 2025** "ConVA" — 黄民烈这条线的代表作 ⭐
6. **Stickland et al. ICML 2024 Spotlight** "Tradeoffs Between Alignment and Helpfulness" — 必读批判
7. **Anthropic 2024 blog** "Refusal in LLMs is mediated by a single direction" — 经验直观

---

## 九、版本

- v1.0  2026-05-18  初版，基于黄民烈 ConVA + 同方向 8 篇代表作
